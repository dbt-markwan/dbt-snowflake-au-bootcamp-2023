version: 2

models:
  - name: stg_fifa__event_type
    description: ""
    columns:
      - name: event_type_id
        tests:
          - unique
          - not_null

  - name: stg_fifa__team
    description: ""
    columns:
      - name: team_id
        tests:
          - unique
          - not_null

  - name: stg_fifa__player
    description: "TEST DESC"
    columns:
      - name: player_id
        description: "Player ID Unique and not Null"
        tests:
          - unique
          - not_null
  
  - name: dim_players
    description: "dim_players desc"
    columns:
      - name: player_id
        description: "Player ID Unique and not Null"
        tests:
          - unique
          - not_null